/**
 * Shared tokens from platform-admin layout kit (liste/boks) for reuse on workplace pages.
 * Kept outside component modules to satisfy react-refresh/only-export-components.
 */

/** White card shell — platform «boks» / standard list box view cards. */
export const WORKPLACE_LAYOUT_BOX_CARD =
  'overflow-hidden rounded-lg border border-neutral-200/80 bg-white shadow-sm'

export const WORKPLACE_LAYOUT_BOX_SHADOW = { boxShadow: '0 1px 2px rgba(0,0,0,0.04)' } as const
